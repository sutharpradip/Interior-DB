{
    "name": "json-server",
    "version": "1.0.0",
    "description": "A simple JSON server",
    "main": "server.js",
    "scripts": {
      "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT"
    },
    "dependencies": {
      "json-server": "^0.17.0"
    }
  }
  
